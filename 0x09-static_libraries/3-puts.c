#include "main.h"
#include <stdio.h>

void _puts(char *s)
{
    (void)s;
    /* My code for the _puts function goes here */
}

int main(void)
{
    puts("\"Programming is like building a multilingual puzzle");
    return (0);
}
